# Руководство пользователя DDDGuard Scanner

Модуль Scanner — это ядро аналитической системы DDDGuard. Он отвечает за обнаружение архитектурных элементов (Bounded Contexts, Layers, Components) и построение графа зависимостей проекта.

Scanner работает в интерактивном режиме через терминальный интерфейс (HUD), позволяя гибко настраивать область анализа перед выполнением ресурсоемких операций.

COMMAND: scan
--------------------------------------------------------------------------------
Описание:
Основной режим работы. Автоматически определяет настройки проекта на основе файла `config.yaml`.
Используется для регулярного анализа архитектуры всего проекта.

Использование:
  dddguard scan

Поведение:
1. Ищет `config.yaml` в текущей директории или выше.
2. Считывает корневую папку проекта и папку исходного кода (`src`).
3. Открывает HUD с уже настроенным путем (Target).

COMMAND: scandir
--------------------------------------------------------------------------------
Описание:
Режим ручного сканирования. Позволяет проанализировать любую произвольную директорию, даже если она не является частью настроенного DDD-проекта.

Использование:
  dddguard scandir

Поведение:
1. Запускает интерактивный выбор пути (Fuzzy Finder).
2. После выбора папки открывает HUD.

ИНТЕРФЕЙС: Scanner HUD (Панель Управления)
--------------------------------------------------------------------------------
Главный экран сканера разделен на информационную панель и интерактивное меню.

Визуализация интерфейса:

╭──────────────────────────── Scanner Configuration ─────────────────────────────╮
│                                                                                │
│                      Target: Project Source (src/dddguard)                     │
│  ────────────────────────────────────────────────────────────────────────────  │
│          Mode:  Python Only                              Layers:  ALL          │
│          Type:  Full Tree                              Contexts:  ALL          │
│       Verbose:  OFF                                       Scope:  Root, Shared │
│                                                                                │
╰───────────────────────────────────────── Arrows to navigate • Enter to toggle ─╯
 (Use arrow keys, Ctrl+C to cancel) 
   FLAGS 
  Verbose Mode         [OFF]
  Directories Only     [OFF]
  Scan All Files       [OFF]
   SCOPE 
  Include Root         [ON]
  Include Shared       [ON]
   FILTERS 
  Filter Layers        [ALL]
  Filter Contexts      [ALL]
   ---------------
      Check Configuration
   ---------------
❯ >>> EXECUTE SCAN <<<
      Exit / Cancel

ОПИСАНИЕ НАСТРОЕК (Меню)
--------------------------------------------------------------------------------

[Секция FLAGS]
Технические параметры сканирования.

* Verbose Mode [OFF/ON]
  Включает вывод отладочной информации в консоль во время сканирования.

* Directories Only [OFF/ON]
  Ускоренный режим. Сканер игнорирует файлы и строит только дерево директорий. Полезно для визуализации структуры папок без анализа кода.

* Scan All Files [OFF/ON]
  - [OFF]: (Рекомендуется) Анализируются только `.py` файлы. Строится AST-граф импортов.
  - [ON]: Включаются все файлы (txt, md, yml, dockerfile). Анализ импортов может быть неточным или отключен для не-Python файлов.

[Секция SCOPE]
Управление видимостью глобальных зон архитектуры.

* Include Root [OFF/ON]
  Включает в анализ Composition Root (файлы в корне source, main.py, di-контейнеры). Если выключено, сканируются только папки контекстов.

* Include Shared [OFF/ON]
  Включает Shared Kernel (общий код). Отключите, чтобы увидеть связи только между бизнес-контекстами.

[Секция FILTERS]
Детальная настройка области анализа.

1. Filter Layers
   Позволяет выбрать конкретные слои для анализа (например, только Domain и App).
   
   Пример экрана выбора слоев:
   ╭─────────────────────────────────────────────────────────────────────────────╮
   │ Select Architectural Layers                                                 │
   ╰──────────────────────── Space to Toggle • Enter to Confirm ─────────────────╯
    
   ❯ [x] Domain
     [x] App
     [x] Driving Adapters
     [x] Driven Adapters
     [x] Driving Dto
     [ ] Driven Dto   <-- Исключен из анализа
     [x] Driving Ports
     [x] Driven Ports
     [x] Composition

2. Filter Contexts
   Позволяет выбрать один или несколько Bounded Contexts. Поддерживает нечеткий поиск.
   
   Пример экрана фильтрации контекстов:
   ╭─────────────────────────────────────────────────────────────────────────────╮
   │ Filter Bounded Contexts                                                     │
   ╰──────────────── Type to search • Tab to Select • Enter to Confirm ──────────╯
   🔍  (Use arrow keys, Ctrl+C to cancel) 
   Filter:  s
   ❯ shared      <-- Результат поиска по "s"
     scanner
     scaffolder
     visualizer

[Секция ACTIONS]

* Check Configuration
  Показывает сводку о том, как DDDGuard видит структуру вашего проекта. Полезно для отладки путей.

  Пример вывода:
  ╭─────── Current Configuration ───────╮
  │                                     │
  │   📍 Project Root:  dddguard        │
  │    📂 Source Code:  ./src/dddguard  │
  │          🧪 Tests:  ./tests         │
  │            📚 Docs:  ./docs          │
  │                                     │
  ╰─────────────────────────────────────╯

* >>> EXECUTE SCAN <<<
  Запускает процесс. Результатом работы является файл `project_tree.json`, содержащий полный граф зависимостей, который используется модулями Linter и Visualizer.

╭────────────────── ✅ Scan Completed Successfully ──────────────────╮
│                                                                    │
│     Contexts Found: 6                                              │
│    Files Processed: 80                                             │
│                                                                    │
│        Output File: project_tree.json                              │
│                                                                    │
╰────────────────────────────────────────────────────────────────────╯
