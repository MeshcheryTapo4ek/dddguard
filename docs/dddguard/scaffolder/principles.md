# Архитектура и Руководство: DDDGuard Scaffolding Engine

> Этот документ описывает устройство и принципы работы движка скаффолдинга (генерации кода) в проекте `dddguard`.

Наш движок представляет собой **Иерархический Композитор Шаблонов**, построенный на принципах DDD (Domain-Driven Design) и использующий шаблонизатор Jinja2.

---

## 1. Ключевая Концепция: Наслаивание Шаблонов

В отличие от традиционных генераторов, которые просто копируют файлы, наш движок **наслаивает** (layers) шаблоны друг на друга. Этот процесс состоит из трёх основных этапов:

1.  **Базовый шаблон (Skeleton):** Определяет общую структуру файлов и директорий (например, `composition.py` с пустым DI-контейнером).
2.  **Функциональные шаблоны (Features):** Добавляют конкретную бизнес-логику или инфраструктурные компоненты (например, провайдеры для доменных сервисов).
3.  **Композиция:** Движок объединяет базовые и функциональные шаблоны, используя стратегии слияния. Шаблон может либо **заменить** файл из предыдущего слоя, либо **внедрить** в него свой код или данные.

---

## 2. Анатомия Шаблона

Каждый шаблон — это директория, содержащая два ключевых элемента:

### 2.1. Манифест (`manifest.json`)

Это паспорт шаблона, который определяет его метаданные и зависимости.

```json
{
  "id": "ctx_business_logic",
  "category": "contexts_base",
  "description": "Контекст с богатой доменной моделью.",
  "dependencies": ["ctx_simple_base"]
}
```

-   `dependencies`: Список ID других шаблонов, которые должны быть применены *до* текущего. Это позволяет выстраивать граф наследования и гарантирует правильный порядок композиции.

### 2.2. Файловая структура (`src/`)

Внутри папки шаблона создается структура, зеркальная целевой структуре проекта.

> **Правило `.jinja`:** Все файлы, предназначенные для рендеринга, обязаны иметь суффикс `.jinja`. Движок автоматически удаляет его при создании финального файла.

**Пример:**
-   **В шаблоне:** `src/{{ root_package }}/config.py.jinja`
-   **Результат:** `src/delivery_context/config.py`

---

## 3. Механизм Композиции

Сервис `TemplateCompositor` выполняет сборку за четыре этапа:

1.  **Разрешение зависимостей (Resolution):** На основе поля `dependencies` в манифестах строится топологически отсортированный список шаблонов для обработки.
    *Пример:* `[utils_loguru, ctx_simple_base, ctx_business_logic]`

2.  **Сбор контекста переменных (Context Building):** Переменные из всех шаблонов собираются в единый словарь `global_variables`. Приоритет при конфликтах (от низкого к высокому):
    -   Метаданные из `manifest.json` родительских шаблонов.
    -   Метаданные из `manifest.json` дочерних шаблонов.
    -   `runtime_variables`, переданные из CLI (например, имя проекта).

3.  **Сбор кандидатов (Candidate Collection):** Движок проходит по всем шаблонам в отсортированном порядке. Для каждого целевого пути (например, `src/my_ctx/composition.py`) он собирает список "кандидатов" — версий этого файла из разных слоёв (шаблонов).

4.  **Рендеринг и применение стратегий (Rendering & Strategy):** Для каждого файла движок решает, как объединить кандидатов, используя метаданные из **Front Matter**.

---

## 4. Управление Композицией: Front Matter

Мы используем формат **Front Matter** (блок YAML в начале файла, ограниченный `---`) для управления поведением движка.

### Синтаксис

```yaml
---
scaffolding: <STRATEGY>
variable_name: value
nested_config:
  key: value
---
<Контент файла (опционально)>
```

### Доступные Стратегии

#### 🅰️ `replace` (По умолчанию)

-   **Поведение:** Текущий файл **полностью заменяет** версию файла из родительского шаблона.
-   **Применение:** Идеально для создания новых файлов или полного переопределения существующих.
-   **Контент:** Всё, что находится после второго разделителя `---`, становится содержимым итогового файла.

#### 🅱️ `add` (Внедрение / Injection)

-   **Поведение:** Содержимое файла (после `---`) игнорируется. Файл служит только контейнером для **переменных**, которые "впрыскиваются" (inject) в контекст рендеринга файла из родительского шаблона.
-   **Применение:** Используется в шаблонах-наследниках для заполнения "дыр" в базовых шаблонах. Например, для добавления провайдеров в DI-контейнер или динамической вставки импортов.

---

## 5. Паттерн "Raw Code Injection"

Это наш основной способ динамического построения кода (например, в файле `composition.py`) без усложнения логики в Jinja-шаблонах.

**Шаг 1: Базовый Шаблон (`ctx_simple_base`)**

Файл `composition.py.jinja` содержит Jinja-плейсхолдеры, но не знает, что в них будет вставлено.

```python
# In: src/{{ root_package }}/composition.py.jinja
class {{ root_package | pascal }}Provider(Provider):
    # ...
    # Сюда будет вставлен код доменных сервисов
    {{ domain_providers_code | default('') }}
```

**Шаг 2: Наследуемый Шаблон (`ctx_business_logic`)**

Файл `composition.py.jinja` использует стратегию `add` и определяет переменную `domain_providers_code` как многострочный блок текста.

```yaml
# In: src/{{ root_package }}/composition.py.jinja
---
scaffolding: add

domain_imports:
  - AssignmentPolicy
  - PricingService

domain_providers_code: |
    @provide
    def provide_pricing_service(self) -> PricingService:
        return PricingService()
---
```

**Результат:** Движок берет контент из базового шаблона, подмешивает в него переменные из дочернего и генерирует валидный Python-код с нужными провайдерами.

---

## 6. Глобальный Контекст и Фильтры

### Глобальные переменные

Эти переменные доступны в любом шаблоне:

-   `root_package`: Имя корневого пакета контекста (в `snake_case`).
-   `project_name`: Имя проекта.
-   `python_version`: Версия Python.

### Кастомные фильтры Jinja2

| Фильтр         | Описание                                                                                                |
| :------------- | :------------------------------------------------------------------------------------------------------ |
| `{{ 'name' | pascal }}` | Преобразует `snake_case` в `PascalCase` (например, `delivery_context` -> `DeliveryContext`). Используется для имен классов. |

---

## 7. Пример Потока Выполнения

Рассмотрим команду:
```bash
dddguard create contexts_base ctx_business_logic
```

1.  **Загрузка:** Движок находит шаблон `ctx_business_logic`.
2.  **Зависимости:**
    -   Находит зависимость `ctx_simple_base` и загружает его.
    -   В `ctx_simple_base` находит зависимость `utils_loguru` и загружает его первым.
    -   Итоговый порядок: `utils_loguru` -> `ctx_simple_base` -> `ctx_business_logic`.
3.  **Обработка `composition.py`:**
    -   **Слой 1 (`simple_base`):** Содержит каркас `Provider` с плейсхолдерами. Стратегия по умолчанию — `replace`.
    -   **Слой 2 (`business_logic`):** Содержит Front Matter со стратегией `add` и переменной `domain_providers_code`.
4.  **Слияние:** Движок берет контент от **Слоя 1** и применяет к нему переменные из **Слоя 2**.
5.  **Рендеринг:** Jinja2 заменяет `{{ domain_providers_code }}` на реальный Python-код.
6.  **Запись:** Финальный файл сохраняется по пути `src/delivery_context/composition.py`.

---

Этот подход позволяет нам поддерживать чистую и гибкую архитектуру шаблонов, где инфраструктура отделена от бизнес-логики.
